<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <div class="Container">
        
        <% if (locals.posts){%>
            <form id="editform" method="post" action="/api/posts/"<%= post.id %>>
                <input type="text" name="title" value="<%= post.title %>">
                <textarea name="content" rows="15"><%= post.content %></textarea>
                <input type="text" name="author" value="<%= post.author %>">
                <button type="submit">
                    <%= submit %>
                </button>
            </form>
        <%} else{%>
            <form action="/api/posts" method="post" id="newPost">
                <input type="text" name="title" placeholder="Title" required>
                <textarea name="content" placeholder="content" rows="15" required></textarea>
                <input type="text" name="author" placeholder="Author" required>
                <button type="submit">
                    <%= submit %>
                </button>
            </form>
            <%}%>
    </div>
    
</body>
</html>